# 📰 Container News Local System

Полная локальная система автоматизации сбора и подготовки новостей по контейнерным технологиям для канала [@DevOps_best_practices](https://t.me/DevOps_best_practices).

## 🎯 Назначение

Система автоматически:
- 🔍 Собирает новости из 50+ RSS источников (Kubernetes, Docker, WebAssembly, eBPF, Edge)
- 🤖 Анализирует важность и категоризирует контент (AI-подход)
- 📝 Генерирует готовые анонсы для Telegram (до 800 символов)
- 📚 Создает детальные аналитические статьи в Markdown
- 📤 Автоматически экспортирует результаты в GitHub репозиторий

## 🏗️ Структура

```
ContainerNewsLocal/
├── README.md                 # Эта инструкция
├── config/                   # Конфигурационные файлы
├── data/                     # База данных и логи
├── scripts/                  # Скрипты автоматизации
├── exports/                  # Готовые материалы для публикации
├── src/                      # Исходный код системы
└── requirements.txt          # Python зависимости
```

## 🚀 Быстрый старт

### 1. Автоматическая установка (Windows)
```cmd
# Запуск установочного скрипта
scripts\setup.bat
```

### 2. Ручная установка
```bash
# Создание виртуального окружения
python -m venv venv
# Windows:
venv\Scripts\activate
# Linux/macOS:
source venv/bin/activate

# Установка зависимостей
pip install -r requirements.txt

# Создание директорий
mkdir data exports exports\telegram exports\articles
```

### 3. Запуск системы
```bash
# Полный цикл (сбор -> экспорт -> синхронизация)
python scripts\run_collection.py

# Или поэтапно:
python src\news_aggregator.py          # Только сбор
python src\operator_interface.py --export-new  # Только экспорт
python src\operator_interface.py --sync-repo   # Синхронизация с репо
```

### 4. Результаты
- **exports/telegram/** - готовые анонсы для Telegram канала
- **exports/articles/** - детальные статьи для экспертного анализа
- **DevOpsBestPractices/container-docker-podman/news/** - автосинхронизация

## ⚙️ Конфигурация

### config/config.yaml
```yaml
# Основные настройки
collection:
  max_age_days: 7           # Возраст новостей
  min_importance: 3         # Минимальная важность для публикации
  
# Экспорт
export:
  telegram_announcements: "exports/telegram/"
  detailed_articles: "exports/articles/"
  repository_path: "../DevOpsBestPractices/container-technologies/news/"
```

## 🔄 Автоматизация

### Cron setup (каждые 4 часа)
```bash
0 */4 * * * cd /path/to/ContainerNewsLocal && python src/news_aggregator.py && python src/operator_interface.py --export-new
```

### Windows Task Scheduler
```powershell
schtasks /create /tn "ContainerNews" /tr "python C:\path\to\ContainerNewsLocal\src\news_aggregator.py" /sc hourly /mo 4
```

## 📤 Публикация результатов

### В репозиторий
```bash
# Копирование готовых материалов
python scripts/sync_to_repo.py

# Результат: файлы появляются в DevOpsBestPractices/container-technologies/news/
```

### В Telegram
1. Откройте **exports/telegram_posts.md**
2. Скопируйте готовые анонсы
3. Опубликуйте в канале [@DevOps_best_practices](https://t.me/DevOps_best_practices)

## 📊 Мониторинг

```bash
# Статистика работы
python src/operator_interface.py --stats

# Проверка источников
python src/news_aggregator.py --test-sources

# Логи
tail -f data/logs/news_system.log
```

## 🔒 Безопасность

- Система работает **только локально**
- Никакой информации о автоматизации не попадает в репозиторий
- В репозитории только **готовые результаты** - анонсы и статьи
- Вся техническая информация остается в локальной системе

---

**⚠️ ВАЖНО**: Не коммитьте эту директорию в публичный репозиторий! Система предназначена для локального использования.